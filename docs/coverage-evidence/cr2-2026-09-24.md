# CR2 sensor-payload evidence, 2026-09-24

The real-file control is raw.pixls.us sample `Canon - EOS 20D - RAW (3:2).CR2`,
SHA-256 `db78660d48589130d99e0bc621424851eb0f84df052634fb5bc89c7a02fe7033`.
The repository API identified the file as CC0 on 2026-09-24, and the downloaded
file matched the declared hash.

The proprietary CR2 header points to the raw IFD at file offset `563216`. The
raw IFD declares old-style JPEG compression code `6` and one sensor strip at
file offset `563728` with length `6925228`. `cr2_sensor.locateSensorPayload`
returns that exact bounded range as `lossless_jpeg`. Its synthetic tests also
pin valid multi-strip layouts to structural rather than fail, and report the
exact strip-offset metadata field when a declared extent starts beyond EOF.

The extracted stream is valid SOF3 lossless Huffman JPEG: 12-bit, 1798 by 2360,
with two components. The jpegz CLI at the tiffz pin
`0004f7473645bfe0ddcd24ae4ee95a2863687f98` returns `unsupported` because the
clean-room SOF3 decoder accepts only one or three components and requires 1x1
sampling factors. rawz sent jpegz the exact fixture, range, and required
syntax-to-end behavior. Random-byte and EOF detection numbers remain unclaimed
until that codec path can consume this Canon layout.
